## This is the non-standard pull request template

Normally, creating PRs should use `.github/pull_request_template.md` if not specified if not one of the other places that the default PR template can be captured.

This is meant to demonstrate where the default template is used versus a non-standard template.
